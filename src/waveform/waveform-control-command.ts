/**
 * Waveform Control command allows you to start (true) or stop (false) the
 * application's components.
 */
export class IWaveformControlCommand {
    /** true to start, false to stop */
    started: boolean;
}
