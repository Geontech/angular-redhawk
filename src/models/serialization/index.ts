/**
 * This is a utility module useful for serializing JSON objects from the server
 * into useful models on the client side.
 */

export { ISerializableFn } from './serializable-fn';
export { ISerializable } from './serializable';
