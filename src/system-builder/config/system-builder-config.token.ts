import { InjectionToken } from '@angular/core';
import { SystemBuilderConfig } from './system-builder-config';

/** System Builder directive's configuration */
export const SYSTEM_BUILDER_CONFIG = new InjectionToken<SystemBuilderConfig>('ar.system-builder-config');
